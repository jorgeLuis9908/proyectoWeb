@page
@model LockoutModel
    <font face="Comic Sans MS,Arial,Verdana" size="6" color="black">
        @{ ViewData["Title"] = "Bloquedo"; }

        <header>
            <h1 class="text-danger">@ViewData["Title"]</h1>
            <p class="text-danger">Esta cuenta ha sido bloqueada Vuelve a intentarlo más tarde.</p>
        </header>
    </font>
